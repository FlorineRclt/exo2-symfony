{% extends 'base.html.twig' %}

{% block metaTitle %}Liste des tags{% endblock %}


{% block main %}
    {% for tag in tags %}
        <a class="tag" style="background-color: {{ tag.color }}" href="{{ path('tagShow', {'id': tag.id}) }}">{{tag.title}}</a>
    {% endfor %}
{% endblock %}



